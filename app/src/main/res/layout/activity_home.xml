<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9FA">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/tvAppTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CatchyW"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#1507D7"
            android:layout_marginBottom="24dp"/>

        <Button
            android:id="@+id/btnStartQuiz"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Sınava Başla"
            android:layout_marginBottom="12dp"
            android:backgroundTint="#CBFB246D"
            app:cornerRadius="12dp"/>

        <Button
            android:id="@+id/btnShowReport"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Başarı Raporu"
            android:layout_marginBottom="12dp"
            android:backgroundTint="#8BC34A"
            app:cornerRadius="12dp"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvWords"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/btnShowReport"
            android:layout_marginTop="10dp"
            android:padding="8dp"/>

        <Button
            android:id="@+id/btnStartWordle"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Kelime Bulmacası (Wordle)"
            android:backgroundTint="#03A9F4"
            app:cornerRadius="12dp"/>


    </LinearLayout>

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/fabAddWord"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="30dp"
        android:text="Kelime Ekle"
        android:textColor="#FFFFFF"
        android:src="@android:drawable/ic_input_add"
        app:backgroundTint="#1507D7"
        app:tint="#FFFFFF" />

</RelativeLayout>